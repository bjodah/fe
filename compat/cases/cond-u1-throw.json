{
	"id": "cond-u1-throw",
	"setup": [
		"(setq cond-u1-flag nil)"
	],
	"expr": "(progn (catch 'tg (unwind-protect (throw 'tg 'x) (setq cond-u1-flag 'ran))) cond-u1-flag)",
	"note": "U1: a cleanup runs on the throw path -- the flag is set during the mid-stack unwind to the catch, and the expression returns ran. Target of Phase 6 (structured errors and non-local exits); pins the completion/condition semantics before the implementing slice lands. Recorded 2026-08-05 against the pinned Emacs 31.0.90 oracle; fe reports its current known gap until the slice flips this feature's status to supported. Slice 06C."
}
