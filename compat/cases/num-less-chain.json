{
 "id": "num-less-chain",
 "setup": [],
 "expr": "(list (< 1 2 3) (< 1 3 2))",
 "note": "05A, row C1: `<` is variadic and chained -- (t nil). Fe today ignores the third operand (fe_eval.c's old ignore-extras bug), so both would be t; 05C's tower makes the comparator chain."
}
