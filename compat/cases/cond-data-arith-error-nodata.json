{
	"id": "cond-data-arith-error-nodata",
	"setup": [],
	"expr": "(condition-case e (/ 1 0) (error e))",
	"note": "Data shape: (/ 1 0) signals (arith-error) with NO data -- the arith-error condition is the bare symbol, unlike (wrong-type-argument ...) which always carries its data. Decision 1's object shape. Target of Phase 6 (structured errors and non-local exits); pins the completion/condition semantics before the implementing slice lands. Recorded 2026-08-05 against the pinned Emacs 31.0.90 oracle; fe reports its current known gap until the slice flips this feature's status to supported. Slice 06D; the producer is Phase 5's arith-error at message level today."
}
