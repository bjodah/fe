{
 "id": "arity-lambda-too-many",
 "setup": [],
 "expr": "(funcall (lambda (x) x) 1 2)",
 "note": "07A A2: extra lambda arguments raise wrong-number-of-arguments with NARGS 2. compare_data is off for this row: the condition symbol and the count agree, but Emacs renders the closure as `#[(x) (x) (t)]` where Fe renders `(lambda (x) x)`, which is a printer Fe does not have rather than a disagreement it could fix (feature phase7-arity-condition-rendering records it, and the *-nargs case beside this one compares the count itself, extracted Lisp-side per 07A Decision 2).",
 "compare_data": false
}
