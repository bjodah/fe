{
	"id": "cond-cc2",
	"setup": [],
	"expr": "(condition-case e (error \"boom\") (error e))",
	"note": "CC2: the handler var is bound to the condition object and the handler returns it; the pinned answer is (error \"boom\") -- the 06A table's '(error boom)' shorthand dropped the string quotes. The condition object shape is Decision 1's (SYMBOL . DATA-LIST). Target of Phase 6 (structured errors and non-local exits); pins the completion/condition semantics before the implementing slice lands. Recorded 2026-08-05 against the pinned Emacs 31.0.90 oracle; fe reports its current known gap until the slice flips this feature's status to supported. Slice 06D."
}
