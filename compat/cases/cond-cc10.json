{
	"id": "cond-cc10",
	"setup": [],
	"expr": "(condition-case nil (signal 'quit nil) (error 'e))",
	"note": "CC10: quit is NOT under error -- an (error ...) handler does not catch (signal 'quit nil), so the quit propagates and the oracle shim records kind quit. Target of Phase 6 (structured errors and non-local exits); pins the completion/condition semantics before the implementing slice lands. Recorded 2026-08-05 against the pinned Emacs 31.0.90 oracle; fe reports its current known gap until the slice flips this feature's status to supported. Slice 06D."
}
