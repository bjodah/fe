{
	"id": "cond-cc6",
	"setup": [],
	"expr": "(condition-case nil (signal 'arith-error nil) (wrong-type-argument 'wta) (arith-error 'ae))",
	"note": "CC6: the first textually-matching handler wins -- the arith-error clause precedes the wrong-type-argument one, so the answer is ae. Target of Phase 6 (structured errors and non-local exits); pins the completion/condition semantics before the implementing slice lands. Recorded 2026-08-05 against the pinned Emacs 31.0.90 oracle; fe reports its current known gap until the slice flips this feature's status to supported. Slice 06D."
}
