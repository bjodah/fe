{
	"id": "cond-data-wrong-type-shape",
	"setup": [],
	"expr": "(condition-case e (car 5) (error e))",
	"note": "Data shape: (car 5) signals (wrong-type-argument listp 5) -- the canonical wrong-type-argument condition. Decision 1's object shape; CheckType becomes this producer (Decision 2). Target of Phase 6 (structured errors and non-local exits); pins the completion/condition semantics before the implementing slice lands. Recorded 2026-08-05 against the pinned Emacs 31.0.90 oracle; fe reports its current known gap until the slice flips this feature's status to supported. Slice 06D."
}
