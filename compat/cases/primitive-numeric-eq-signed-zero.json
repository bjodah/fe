{
 "id": "primitive-numeric-eq-signed-zero",
 "setup": [],
 "expr": "(= 0.0 -0.0)",
 "note": "02A: pins that numeric `=` treats positive and negative zero as equal (IEEE 754 `==`, which Fe's existing doubles already use in its other comparison primitives), so t. Fe's `=` is still assignment today, so standalone Fe evaluates a different shape; this is the expected gap the planned row records."
}
