{
 "id": "vector-aset-out-of-range",
 "setup": [],
 "expr": "(let ((v (make-vector 2 0))) (aset v 2 1))",
 "note": "The write side of the bounds check, with the same condition and data shape as the read side. Index 2 on a length-2 vector is the off-by-one an implementation writing `index <= length' produces, and it would corrupt the payload block beyond the vector rather than raise."
}
