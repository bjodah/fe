{
 "id": "arity-lambda-after-operands",
 "setup": [],
 "expr": "(condition-case nil (progn (setq n 0) ((lambda (x) x) (setq n 1) (setq n 2))) (error n))",
 "note": "07A A7: lambda operands evaluate left-to-right before binding diagnoses arity, so the caught result is 2. Planned Fe gap until 07B."
}
