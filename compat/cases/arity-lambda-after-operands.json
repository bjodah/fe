{
 "id": "arity-lambda-after-operands",
 "setup": [],
 "expr": "(condition-case nil (do (setq n 0) ((lambda (x) x) (setq n 1) (setq n 2))) (error n))",
 "note": "07B: lambda operands evaluate left-to-right before binding diagnoses arity, so the caught result is 2."
}
