{
 "id": "lisp2-value-function-coexist",
 "setup": [
  "(setq f 7)",
  "(fset (quote f) (lambda () 9))"
 ],
 "expr": "(list f (f))",
 "note": "04C: the parent plan's headline now works -- one symbol `f` holds a value (7) and a function (an fset'd lambda) at once, call position resolves the function cell while bare-symbol evaluation resolves the value cell, so the list is (7 9)."
}
