{
 "id": "reader-sharp-quote-identity",
 "setup": [],
 "expr": "(quote #'car)",
 "note": "`#'car` reads as `(function car)`, and fe's writer prints `(function car)` as `#'car` -- the same abbreviation Emacs' printer uses -- so quoting it prints identically in both. The pre-04D reader treated `#'` as identity and printed the bare symbol, which is what recorded this case as a gap."
}
