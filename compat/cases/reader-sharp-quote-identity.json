{
 "id": "reader-sharp-quote-identity",
 "setup": [],
 "expr": "(quote #'car)",
 "note": "fe.c's reader treats `#'` as insignificant and reads `#'car` as the bare symbol `car` (documented in doc/fe-upstream.md). Emacs reads `#'car` as `(function car)`, so quoting it prints differently in each: fe prints the bare symbol, Emacs prints the `function` form."
}
