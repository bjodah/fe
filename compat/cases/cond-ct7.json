{
	"id": "cond-ct7",
	"setup": [
		"(setq cond-ct7-flag nil)"
	],
	"expr": "(progn (catch 'tg (unwind-protect (throw 'tg 'thrown) (setq cond-ct7-flag 'ran))) cond-ct7-flag)",
	"note": "CT7: a throw through unwind-protect runs the cleanup -- the flag is set on the throw path and the catch returns the flag value ran. Target of Phase 6 (structured errors and non-local exits); pins the completion/condition semantics before the implementing slice lands. Recorded 2026-08-05 against the pinned Emacs 31.0.90 oracle; fe reports its current known gap until the slice flips this feature's status to supported. Slice 06C."
}
