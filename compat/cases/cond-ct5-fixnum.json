{
	"id": "cond-ct5-fixnum",
	"setup": [],
	"expr": "(catch 5 (throw 5 'hit) 'miss)",
	"note": "CT5: tag comparison is eq -- a fixnum tag matches an equal fixnum (Phase 5's integer eq is load-bearing), answering hit. Target of Phase 6 (structured errors and non-local exits); pins the completion/condition semantics before the implementing slice lands. Recorded 2026-08-05 against the pinned Emacs 31.0.90 oracle; fe reports its current known gap until the slice flips this feature's status to supported. Slice 06C."
}
