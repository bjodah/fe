{
 "id": "lisp2-symbol-value",
 "setup": [
  "(setq sv 5)"
 ],
 "expr": "(symbol-value (quote sv))",
 "note": "04A: `symbol-value` reads the value cell directly and returns 5. Fe has no `symbol-value` today; the value-cell read is what bare-symbol evaluation already does, but the explicit primitive is the namespace-split contract."
}
