{
	"id": "cond-s3",
	"setup": [],
	"expr": "(condition-case e (signal 'nonexistent-condition '(1)) (error e))",
	"note": "S3: an unregistered condition symbol is an ordinary (error \"Invalid error symbol\" nonexistent-condition), catchable by an (error ...) handler. Target of Phase 6 (structured errors and non-local exits); pins the completion/condition semantics before the implementing slice lands. Recorded 2026-08-05 against the pinned Emacs 31.0.90 oracle; fe reports its current known gap until the slice flips this feature's status to supported. Slice 06D."
}
