{
	"id": "cond-u3",
	"setup": [
		"(setq cond-u3-l nil)"
	],
	"expr": "(progn (condition-case nil (unwind-protect (unwind-protect (error \"boom\") (push 'in cond-u3-l)) (push 'out cond-u3-l)) (error 'caught)) cond-u3-l)",
	"note": "U3: nested unwind-protect runs innermost-first -- in is pushed before out, so the final list is (out in). Target of Phase 6 (structured errors and non-local exits); pins the completion/condition semantics before the implementing slice lands. Recorded 2026-08-05 against the pinned Emacs 31.0.90 oracle; fe reports its current known gap until the slice flips this feature's status to supported. Slice 06D."
}
