{
 "id": "reader-no-integers",
 "setup": [],
 "expr": "42.0",
 "note": "Sub-plan 05D's cut flipped this to supported un-regenerated: the case's pinned answer is that the float literal 42.0 prints as \"42.0\", which the old integral-double writer shortcut rendered as \"42\". Fe now prints floats with their explicit decimal point (05A Decision 4), and 42 reads as an integer, so the two spellings never collide."
}
