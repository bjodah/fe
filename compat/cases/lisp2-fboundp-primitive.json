{
 "id": "lisp2-fboundp-primitive",
 "setup": [],
 "expr": "(fboundp (quote car))",
 "note": "04A: `fboundp` asks the function namespace, and the primitive `car` lives there, so it is t. The 04D cut moved the bootstrap into function cells, giving fe the same answer; `(boundp 'car)` (one-namespace-boundp) is nil, the mirror on the value side."
}
