{
	"id": "unsupported-hash-table-p",
	"setup": [],
	"expr": "(hash-table-p (make-hash-table))",
	"note": "Fe has no hash-table type, reader syntax, or primitives at all. The runner short-circuits on this case's `unsupported` status instead of invoking Fe. On the actual function channel `(hash-table-p ...)` is plain `void-function`, byte-identical to a typo; Fe has no curated known-missing-function channel (10A Decision 5)."
}
