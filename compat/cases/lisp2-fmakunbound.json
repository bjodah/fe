{
 "id": "lisp2-fmakunbound",
 "setup": [
  "(fset (quote j) (lambda () 1))",
  "(fmakunbound (quote j))"
 ],
 "expr": "(j)",
 "note": "04A: `fmakunbound` empties only the function cell, so the subsequent call signals void-function j. Fe has no `fmakunbound` (nor `fset`) today."
}
