{
 "id": "arity-condition-quote-rendering",
 "setup": [],
 "expr": "(condition-case e (quote 1 2) (wrong-number-of-arguments (cdr e)))",
 "note": "The whole data list of `(quote 1 2)`'s arity condition, printed. Both sides hold `(quote 2)`; Emacs' printer abbreviates it to `'2` and Fe's writes it out. A writer divergence, not an arity one."
}
