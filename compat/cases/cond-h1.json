{
	"id": "cond-h1",
	"setup": [],
	"expr": "(get 'arith-error 'error-conditions)",
	"note": "H1: the hierarchy rows, as Emacs' error-conditions property spells them -- (arith-error error). Phase 6 Decision 1 deliberately keeps Fe's hierarchy as static C data, so `get`/`error-conditions` stay unsupported rather than planned; the actual Fe call is plain `void-function get`, indistinguishable from a typo (10A Decision 5). The row preserves the Emacs 31.0.90 oracle the static table was built against; cond-cc5, cond-cc8 and cond-ct6 exercise the hierarchy Fe does expose."
}
