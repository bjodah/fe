{
 "id": "lisp2-funcall-lexical-value",
 "setup": [],
 "expr": "(let ((g (lambda (x) (+ x 1)))) (funcall g 3))",
 "note": "04A: `funcall` takes a *value* -- here the closure bound by `let` to g -- and calls it, so the function cell of g is irrelevant and the answer is 4. 04C lands `funcall`, but fe's `let` still takes a bare symbol (primitive-let), so the Emacs-shaped bindings list needs the prelude's Emacs-shaped `let` (04E); stays planned."
}
